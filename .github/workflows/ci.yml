name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  tests:
    name: Tests (PHP ${{ matrix.php }} on ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        php: ['8.5']
        os: [ubuntu-latest, windows-latest]
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          extensions: ffi, json, hash, fileinfo, pdo, pdo_sqlite, zip, sodium
          coverage: none
      - run: composer install --no-progress --prefer-dist
      - run: composer cs-check
      - run: composer stan
      - run: composer psalm
      - run: composer test

  integration:
    name: Integration Tests
    runs-on: ubuntu-latest
    if: false
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.5'
          extensions: ffi, json, hash, fileinfo, pdo, pdo_sqlite
      - run: echo 'Integration tests require ONNX Runtime and llama.cpp native libraries'
